from setuptools import setup

setup(
    name='geo',
    version='0.2.0',
    author='DrMentats',
    license='MIT',
    packages=['geo'],
    install_requires=[
        'numpy',
        'terminaltables',
    ],
)
