from setuptools import setup

setup(
    name='geo',
    version='0.2.1-alpha',
    author='DrMentats',
    license='MIT',
    packages=['geo'],
    install_requires=[
        'numpy',
        'terminaltables',
    ],
)
