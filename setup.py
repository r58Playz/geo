from setuptools import setup

setup(
    name='geo',
    version='0.3.0-alpha',
    author='DrMentats',
    license='MIT',
    packages=['geo'],
    install_requires=[
        'numpy',
        'terminaltables',
    ],
)
