from setuptools import setup

setup(
    name='geo',
    version='0.1.0',
    author='DrMentats',
    license='MIT',
    packages=['geo'],
    install_requires=[
        'numpy',
        'terminaltables',
    ],
)
